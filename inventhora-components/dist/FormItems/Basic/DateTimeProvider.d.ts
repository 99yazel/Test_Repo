import { FC } from 'react';
import { Language } from '../../lib/types';
declare const DateTimeProvider: FC<Props>;
export default DateTimeProvider;
export interface Props {
    lang: Language;
}
